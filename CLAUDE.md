# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

As of this writing, the repository is a fresh, empty project. It contains only `README.md` ("mayfirst — Mein erstes Rep") and has no source code, build system, tests, dependencies, or CI configuration. There is nothing meaningful to document about architecture or workflows yet.

When source code is added, replace the sections below with real content (build/lint/test commands, how to run a single test, and the big-picture architecture that spans multiple files). Until then, do not invent commands or conventions that aren't backed by files in the repo.

## Branch convention

Development for AI-assisted changes happens on the branch specified by the task (e.g. `claude/add-claude-documentation-188mL`). Push only to the designated branch and open a draft PR.
